import tkinter as tk
def show_orders_window(root):
    orders_window = tk.Toplevel(root)
    orders_window.title("Orders")
    orders_window.geometry("400x300")

    # Display orders list and options here
